apiVersion: v1
kind: Service
metadata:
  name: {{ include "gatau.fullname" . }}
  labels:
    {{- include "gatau.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gatau.selectorLabels" . | nindent 4 }}
